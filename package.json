{
  "name": "quiz-vite-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "pnpm run build",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "firebase": "^12.3.0"
  }
}